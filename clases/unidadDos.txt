## 🟦 UNIDAD 2 – TABLAS Y ESTRUCTURAS BÁSICAS

### 2.1 CREAR TABLAS

---

### ✅ 2a. Concepto de tabla, campos, filas y columnas

En bases de datos, una **tabla** es una estructura que almacena información organizada en **columnas** (también llamadas campos) y **filas** (también llamadas registros).

* **Tabla**: es un conjunto de datos relacionados, almacenados de forma estructurada.
* **Campos o columnas**: cada campo define una categoría de datos, como “nombre”, “edad” o “precio”.
* **Filas o registros**: cada fila contiene un conjunto de valores que representan una instancia concreta, como una persona, producto o pedido.

Podés imaginarlo como una hoja de Excel:

* Las columnas son los títulos de cada dato.
* Las filas son los datos reales, una por cada ítem o persona.

#### 🧪 Ejemplo práctico:

```sql
CREATE TABLE empleados (
  id INT,
  nombre VARCHAR(100),
  edad INT,
  email VARCHAR(100)
);
```

Aquí creás una tabla llamada `empleados` con 4 columnas:

* `id`: número identificador.
* `nombre`: texto de hasta 100 caracteres.
* `edad`: número entero.
* `email`: texto.

#### 🏋️‍♂️ Ejercicio:

Creá una tabla llamada `cursos` con los siguientes campos:

* `id` (INT)
* `nombre` (VARCHAR(150))
* `duracion` (INT, en horas)

```sql
CREATE TABLE cursos (
  id INT,
  nombre VARCHAR(150),
  duracion INT
);
```

---

### ✅ 2b. Tipos de datos (INT, VARCHAR, TEXT, DATE, BOOLEAN, etc.)

Al crear una tabla, es necesario definir **el tipo de dato** para cada campo. Esto le indica a MySQL qué clase de información esperar.

#### 🔸 Tipos de datos más comunes:

| Tipo                      | Descripción                        | Ejemplo de uso             |
| ------------------------- | ---------------------------------- | -------------------------- |
| `INT`                     | Números enteros                    | edad, cantidad             |
| `DECIMAL(10,2)` o `FLOAT` | Números con decimales              | precios, puntajes          |
| `VARCHAR(n)`              | Texto corto (hasta `n` caracteres) | nombres, correos           |
| `TEXT`                    | Texto largo (sin límite definido)  | descripciones, comentarios |
| `DATE`                    | Fecha (formato YYYY-MM-DD)         | fecha de nacimiento        |
| `DATETIME`                | Fecha y hora                       | registros de eventos       |
| `BOOLEAN` o `TINYINT(1)`  | Valor verdadero (1) o falso (0)    | activo/inactivo, aprobado  |

#### 🧪 Ejemplo:

```sql
CREATE TABLE productos (
  id INT,
  nombre VARCHAR(100),
  descripcion TEXT,
  precio DECIMAL(10,2),
  disponible BOOLEAN,
  fecha_ingreso DATE
);
```

Esta tabla guarda productos con nombre, descripción, precio, si está disponible o no, y cuándo fue ingresado.

#### 🏋️‍♂️ Ejercicio:

Creá una tabla `alumnos` con los campos:

* `id` (INT)
* `nombre` (VARCHAR 100)
* `aprobado` (BOOLEAN)
* `ingreso` (DATE)

```sql
CREATE TABLE alumnos (
  id INT,
  nombre VARCHAR(100),
  aprobado BOOLEAN,
  ingreso DATE
);
```

---

### ✅ 2c. Clave primaria (PRIMARY KEY) y auto incrementos (AUTO\_INCREMENT)

Una **clave primaria** (PRIMARY KEY) identifica de forma única cada fila dentro de una tabla. Es un campo obligatorio, único y no puede contener valores `NULL`.

El modificador **AUTO\_INCREMENT** permite que MySQL aumente automáticamente el valor de ese campo en cada nueva fila.

#### 🧪 Ejemplo:

```sql
CREATE TABLE clientes (
  id INT PRIMARY KEY AUTO_INCREMENT,
  nombre VARCHAR(100),
  email VARCHAR(100)
);
```

Esto significa que cada nuevo cliente tendrá un `id` automático que comienza en 1 y aumenta en 1 con cada inserción.

#### ⚠️ Importancia:

* Evita duplicados.
* Facilita relaciones entre tablas.
* Garantiza la integridad de los datos.

#### 🏋️‍♂️ Ejercicio:

Creá una tabla `categorias` con:

* `id` como clave primaria y autoincrementable.
* `nombre` como campo de texto.

```sql
CREATE TABLE categorias (
  id INT PRIMARY KEY AUTO_INCREMENT,
  nombre VARCHAR(100)
);
```

Agregá dos categorías:

```sql
INSERT INTO categorias (nombre) VALUES ('Electrónica'), ('Hogar');
```

✅ Resultado esperado:

| id | nombre      |
| -- | ----------- |
| 1  | Electrónica |
| 2  | Hogar       |

---

Cuando quieras, seguimos con la **Unidad 2.2** o cualquier otro tema que necesites. ¿Querés que veamos cómo insertar datos o cómo hacer relaciones entre tablas?

////////////////////////////////////////////////////////////////////

## 🟦 UNIDAD 2.2 – MODIFICAR TABLAS

---

### ✅ 2a. Agregar, modificar o borrar columnas

Las tablas se pueden modificar fácilmente con el comando `ALTER TABLE`. Este permite:

#### 🟢 Agregar una columna:

```sql
ALTER TABLE productos ADD marca VARCHAR(100);
```

Esto agrega una columna llamada `marca` al final de la tabla `productos`.

#### 🟡 Modificar una columna:

```sql
ALTER TABLE productos MODIFY marca VARCHAR(150);
```

Esto cambia el tipo o tamaño de la columna `marca`.

#### 🔴 Borrar una columna:

```sql
ALTER TABLE productos DROP COLUMN marca;
```

Esto elimina completamente la columna.

#### 🧪 Ejercicio:

1. Creamos una tabla simple:

```sql
CREATE TABLE libros (
  id INT PRIMARY KEY AUTO_INCREMENT,
  titulo VARCHAR(100)
);
```

2. Agregamos una columna `autor`:

```sql
ALTER TABLE libros ADD autor VARCHAR(100);
```

3. Modificamos la columna `autor` para aumentar su tamaño:

```sql
ALTER TABLE libros MODIFY autor VARCHAR(200);
```

4. Borramos la columna:

```sql
ALTER TABLE libros DROP COLUMN autor;
```

---

### ✅ 2b. Tipos de restricciones (`NOT NULL`, `UNIQUE`, `DEFAULT`)

Las **restricciones** controlan cómo se insertan y mantienen los datos en las tablas.

#### 🔸 `NOT NULL`:

Evita que una columna tenga valores vacíos.

```sql
nombre VARCHAR(100) NOT NULL
```

#### 🔸 `UNIQUE`:

Impide que se repitan valores en esa columna.

```sql
email VARCHAR(100) UNIQUE
```

#### 🔸 `DEFAULT`:

Asigna un valor por defecto si no se especifica otro.

```sql
activo BOOLEAN DEFAULT TRUE
```

#### 🧪 Ejemplo completo:

```sql
CREATE TABLE usuarios (
  id INT PRIMARY KEY AUTO_INCREMENT,
  nombre VARCHAR(100) NOT NULL,
  email VARCHAR(100) UNIQUE,
  registrado DATE DEFAULT CURRENT_DATE
);
```

#### 🏋️‍♂️ Ejercicio:

Modificá la tabla `libros` para agregar una columna `precio` con valor por defecto de 100:

```sql
ALTER TABLE libros ADD precio DECIMAL(10,2) DEFAULT 100.00;
```

---

### ✅ 2c. Eliminar tablas o bases de datos (con cuidado)

⚠️ ¡Estas operaciones borran datos para siempre!

#### 🔥 Eliminar una tabla:

```sql
DROP TABLE productos;
```

#### 🔥 Eliminar una base de datos:

```sql
DROP DATABASE tienda;
```

#### 🧪 Buenas prácticas:

* Revisar bien los nombres.
* Hacer respaldo si es necesario.
* Asegurarte de no tener relaciones activas con otras tablas.

#### 🏋️‍♂️ Ejercicio:

Eliminá una tabla temporal:

```sql
CREATE TABLE temporal (id INT);
DROP TABLE temporal;
```

---

### ✅ 2d. Insertar datos

Una vez creada la tabla, se insertan datos con `INSERT INTO`.

#### 🧪 Ejemplo:

```sql
INSERT INTO libros (titulo, precio) VALUES ('Cien años de soledad', 1800.00);
```

Podés insertar varios registros a la vez:

```sql
INSERT INTO libros (titulo, precio) VALUES 
('El Principito', 900.00),
('1984', 1200.50),
('Rayuela', 1500.00);
```

#### 🏋️‍♂️ Ejercicio:

Insertá 2 usuarios en la tabla `usuarios`:

```sql
INSERT INTO usuarios (nombre, email) VALUES 
('Juan Pérez', 'juan@example.com'),
('Ana García', 'ana@example.com');
```

---

### ✅ 2e. Hacer relaciones entre tablas

Una **relación** permite conectar datos entre dos o más tablas. Se hace con **llaves foráneas** (`FOREIGN KEY`).

#### 📘 Supongamos:

* Una tabla `categorias`.
* Una tabla `productos` que pertenece a una categoría.

#### 🧪 Paso a paso:

1. Crear la tabla `categorias`:

```sql
CREATE TABLE categorias (
  id INT PRIMARY KEY AUTO_INCREMENT,
  nombre VARCHAR(100)
);
```

2. Crear la tabla `productos` con relación:

```sql
CREATE TABLE productos (
  id INT PRIMARY KEY AUTO_INCREMENT,
  nombre VARCHAR(100),
  categoria_id INT,
  FOREIGN KEY (categoria_id) REFERENCES categorias(id)
);
```

Ahora, cada producto queda asociado a una categoría específica por `categoria_id`.

#### 🧪 Insertar datos:

```sql
INSERT INTO categorias (nombre) VALUES ('Electrónica'), ('Ropa');

INSERT INTO productos (nombre, categoria_id) 
VALUES ('Smartphone', 1), ('Campera', 2);
```

#### 🏋️‍♂️ Ejercicio:

1. Crear una tabla `clientes`.
2. Crear una tabla `pedidos` que tenga un `cliente_id` que se relacione con la tabla `clientes`.

```sql
CREATE TABLE clientes (
  id INT PRIMARY KEY AUTO_INCREMENT,
  nombre VARCHAR(100)
);

CREATE TABLE pedidos (
  id INT PRIMARY KEY AUTO_INCREMENT,
  producto VARCHAR(100),
  cliente_id INT,
  FOREIGN KEY (cliente_id) REFERENCES clientes(id)
);
```5151465561651651561651